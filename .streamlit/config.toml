# Streamlit app configuration (used by `streamlit run` and Streamlit Cloud).
[theme]
base = "light"
primaryColor = "#2a78d6"   # matches the charts' day-ahead blue

[browser]
gatherUsageStats = false
